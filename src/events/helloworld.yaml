'/helloworld.http.get': # event id. Will include path params. For ex. com.abc.do_kyc/{bank_id}/process/{user_id}
  fn: com.jfs.helloworld
  summary: hello world
  description: print hello world
  produces:
    - application/json
  parameters:
    - name: status
      in: query
      schema:
        type: string
  responses:
    '200':
      description: Returns the greeting.
      schema:
        type: string
    '400':
      description: Invalid status value
 

