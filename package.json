{
  "name": "scaffoldinggodspeed",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "PORT=3000 nodemon gs_service/dist/app.js",
    "clean": "cd gs_service && npm run clean",
    "build": "rsync --delete -rv src/ gs_service/project && npm i && cd gs_service && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "nodemon": "^2.0.15"
  }
}
